import React from 'react';
import Layout from '../components/Layout/Layout';

const HomePage = () => {
    return (
        <Layout>
            valami
        </Layout>
    )
};

export default HomePage;